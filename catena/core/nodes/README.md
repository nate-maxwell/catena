# Nodes

Root directory contains the base `CatenaNode` class, the comment box node class,
and the graph itself.

Each individual node exists in a subfolder for each node category. Each category
`__init__` contains the color for nodes of that category.
